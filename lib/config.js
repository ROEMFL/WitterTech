// Site-wide flag. When true, the public sees only a "coming soon" home page;
// every other route redirects to it (see middleware.js), and the chrome,
// sitemap, and robots adjust accordingly. Flip to false to relaunch the full site.
export const COMING_SOON = true
